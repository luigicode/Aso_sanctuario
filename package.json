{
  "name": "aso",
  "version": "1.0.0",
  "description": "aso_sanc",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Luigi",
  "license": "ISC",
  "dependencies": {
    "node": "^14.11.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
